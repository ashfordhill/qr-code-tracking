{
  "name": "qr-code-tracking",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 execute qr-tracking --local --file=schema.sql",
    "db:init:remote": "wrangler d1 execute qr-tracking --remote --file=schema.sql"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.101.0"
  }
}
